<?php
$option = $_GET['mo'];
$event = $_GET['event'];

if ($option === "Edit"){
die;
}
if ($option === "Delete"){

die;
}
header('Location: ../upload.php');
?>


Make delete and edit options for boxoffice items
